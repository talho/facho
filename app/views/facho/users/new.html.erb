<div class="t_boot">
  <h2>FACHO Dashboard sign-up form</h2>
  <% form_for @user, :url => facho_users_path, :html => {:class => "form-stacked"}  do |f| %>
    <%= render 'new_facho_user_form', :f => f, :user => @user %>
    <div>
      <%= f.submit 'Sign Up', :disable_with => "Please wait...", :class => "btn large primary" %> or <%= link_to 'Cancel', sign_in_path, :class => "btn large" %>
    </div>
  <% end %>
</div>